// Script encargado de las rutas de cada producto/servicio de la API

import { Router } from "express";

const router = Router()

router.get((req,
     res) => res.send('No se encontro tu pagina'))



export default router